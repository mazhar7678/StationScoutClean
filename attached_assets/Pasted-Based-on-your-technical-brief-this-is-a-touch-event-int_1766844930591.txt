Based on your technical brief, this is a touch event interception issue on Android. Here are the most likely fixes:

Most Likely Solution: Pressable with Explicit Props
Replace your button with a basic Pressable to rule out React Native Paper issues:

jsx
import { Pressable, Text, StyleSheet } from 'react-native';

<Pressable
  onPress={() => {
    console.log('PRESSED');
    Alert.alert('Button pressed!');
    handleLogin();
  }}
  style={({ pressed }) => [
    styles.button,
    pressed && { opacity: 0.7 }
  ]}
  android_ripple={{ color: 'rgba(255,255,255,0.3)' }}
>
  <Text style={styles.buttonText}>Sign In</Text>
</Pressable>

Check These Parent Container Issues
1. Remove any pointerEvents props entirely:

jsx
// ❌ Remove these from ALL parent Views
pointerEvents="box-none"
pointerEvents="none"

2. Check KeyboardAvoidingView setup:

jsx
// Try removing KeyboardAvoidingView temporarily to test
// Or ensure it's configured correctly:
<KeyboardAvoidingView 
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
  style={{ flex: 1 }}
>

3. ScrollView fix:

jsx
<ScrollView
  keyboardShouldPersistTaps="handled"  // Try "handled" instead of "always"
  contentContainerStyle={{ flexGrow: 1 }}
>

Quick Diagnostic Build
Create a minimal test screen to isolate the issue:

jsx
// Replace your entire LoginScreen temporarily with:
import React from 'react';
import { View, Pressable, Text, Alert, StyleSheet } from 'react-native';

export default function LoginScreen() {
  return (
    <View style={styles.container}>
      <Pressable
        onPress={() => Alert.alert('It works!')}
        style={styles.button}
      >
        <Text style={styles.text}>TEST BUTTON</Text>
      </Pressable>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  button: { backgroundColor: 'blue', padding: 20, borderRadius: 8 },
  text: { color: 'white', fontSize: 18 },
});

If this works: The issue is in your view hierarchy or React Native Paper.
If this doesn't work: The issue is deeper (Hermes/build config).

Hermes-Specific Fix
Add this to your app.json:

json
{
  "expo": {
    "jsEngine": "hermes",
    "experiments": {
      "reactCanary": false
    }
  }
}

What to do now:

Try the minimal test screen first
Report back if that button works
Then we'll know where the problem is